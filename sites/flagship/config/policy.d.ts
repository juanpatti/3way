// Type declarations for policy.js — see the note at the top of seed.d.ts in this same
// directory for why this file exists and how TS resolves it.
import type { PolicyRules } from '../../../packages/widget/src/types';

export const POLICY_PROSE: string;
export const POLICY_RULES: PolicyRules;
